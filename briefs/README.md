# Brief drop folder

Commit validated files here as `YYYY-MM-DD.json`. Use
`examples/brief.sample.json` as the authoring reference. A JSON change on `main`
starts the PDF build workflow; other files in this directory do not.

